# WATI (WhatsApp broadcast API) bearer token. Required for /api/wati/send-broadcast.
# Get this from the WATI dashboard for the relevant tenant.
WATI_BEARER=

# Port the server listens on (defaults to 5000 if unset)
PORT=5000
